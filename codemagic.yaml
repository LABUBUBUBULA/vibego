workflows:
  ios-project-setup:
    name: iOS Project Setup
    max_build_duration: 30
    instance_type: mac_mini_m2
    environment:
      xcode: latest
    scripts:
      - name: Create Xcode project
        script: |
          echo "Remote access enabled - waiting for VNC connection..."
          sleep 1800
